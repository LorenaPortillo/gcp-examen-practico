apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ventas-ef
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ventas-ef
                port:
                  number: 80
          - path: /ventas
            pathType: Prefix
            backend:
              service:
                name: ventas-ef
                port:
                  number: 80
